name: Build and Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Build Windows executable
      run: |
        pyinstaller --onefile --windowed --icon=app_icon.ico anti_adhd.py
        
    - name: Sign Windows executable
      run: |
        # 코드 서명을 위한 스크립트 (실제 서명은 별도의 인증서 필요)
        # signtool sign /tr http://timestamp.digicert.com /td sha256 /fd sha256 /a dist/anti_adhd.exe
        
    - name: Upload Windows artifact
      uses: actions/upload-artifact@v3
      with:
        name: anti-adhd-windows
        path: dist/anti_adhd.exe

  build-macos:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Build macOS app
      run: |
        pyinstaller --onefile --windowed --icon=app_icon.icns anti_adhd.py
        
    - name: Sign macOS app
      run: |
        # 코드 서명을 위한 스크립트 (실제 서명은 별도의 인증서 필요)
        # codesign --force --sign "Developer ID Application: Your Name" dist/anti_adhd
        
    - name: Upload macOS artifact
      uses: actions/upload-artifact@v3
      with:
        name: anti-adhd-macos
        path: dist/anti_adhd

  build-linux:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Build Linux executable
      run: |
        pyinstaller --onefile --windowed anti_adhd.py
        
    - name: Upload Linux artifact
      uses: actions/upload-artifact@v3
      with:
        name: anti-adhd-linux
        path: dist/anti_adhd

  create-release:
    needs: [build-windows, build-macos, build-linux]
    runs-on: ubuntu-latest
    steps:
    - name: Download all artifacts
      uses: actions/download-artifact@v3
      with:
        path: artifacts
        
    - name: Create Release
      id: create_release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          artifacts/anti-adhd-windows/anti_adhd.exe
          artifacts/anti-adhd-macos/anti_adhd
          artifacts/anti-adhd-linux/anti_adhd
        body_path: RELEASE_NOTES.md
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} 